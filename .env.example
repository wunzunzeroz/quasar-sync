# PostgreSQL connection string for the raw data database (no schema suffix)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Base64-encoded SSH private key for Kart repository access
# Generate with: base64 < ~/.ssh/id_ed25519
SSH_PRIVATE_KEY=

# Optional: Log level (debug, info, warn, error)
LOG_LEVEL=info
